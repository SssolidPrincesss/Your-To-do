<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Превью темы -->
        <ImageView
            android:id="@+id/theme_preview"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:scaleType="centerCrop"
            android:background="@color/gray"
            android:contentDescription="Превью темы" />

        <!-- Затемнение для заблокированных тем -->
        <View
            android:id="@+id/theme_overlay"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:background="@drawable/theme_overlay_dark"
            android:visibility="gone" />

        <!-- Название темы (поверх превью) -->
        <TextView
            android:id="@+id/theme_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="6dp"
            android:background="@drawable/theme_name_background"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:layout_gravity="center"
            android:layout_marginTop="20dp" />

        <!-- Иконка замка -->
        <ImageView
            android:id="@+id/lock_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_close"
            android:layout_gravity="center"
            android:visibility="gone"
            app:tint="@color/white" />

        <!-- Статус блокировки (под превью) -->
        <TextView
            android:id="@+id/status_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="100dp"
            android:textSize="14sp"
            android:textColor="@color/white"
            android:visibility="gone" />

        <!-- Маркер выбранной темы -->
        <ImageView
            android:id="@+id/selected_checkmark"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_home"
            android:layout_gravity="top|end"
            android:layout_margin="12dp"
            android:visibility="gone" />
    </FrameLayout>
</com.google.android.material.card.MaterialCardView>