<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Фоновый круг -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#1D1818" />
            <size
                android:width="24dp"
                android:height="24dp" />
        </shape>
    </item>

    <!-- Галочка (вектор) -->
    <item
        android:width="14dp"
        android:height="14dp"
        android:drawable="@drawable/ic_check_vector"
        android:gravity="center" />
</layer-list>